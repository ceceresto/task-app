[phases.setup]
nixPkgs = ["php84", "php84Extensions.pdo_pgsql", "nodejs_20"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install",
  "npm run build"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -8080"